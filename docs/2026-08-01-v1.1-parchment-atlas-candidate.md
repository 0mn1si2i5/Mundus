# V1.1.0 Parchment Atlas candidate evidence

Evidence recorded 2026-08-01 for Draft
[PR #5](https://github.com/0mn1si2i5/Mundus/pull/5), branch
`codex/v1.1-parchment-convergence`. The public V1.0.0 baseline and live `main`
remain `a5ff99bc60fb7cd2e6e14f4d3bc4f54e5abfb4a1`.

## Candidate state

Complete manual production-preview acceptance reran at the exact final
implementation HEAD `d54481e22a9bb33e3f4705acfd87f4016a86d7e0` from
2026-08-01T11:49:23.164Z through 2026-08-01T11:50:40.799Z
(19:49:23-19:50:40 CST). All 12 approved checklist items passed. This closes
the exact-final-implementation acceptance and literal subpath gates; this
evidence-only change will create a later documentation head.

Environment: Node.js 22.23.1, pnpm 11.7.0, Chromium 149.0.7827.55; desktop
1280x720 at DPR 1; Pixel emulation viewport 412x839 at DPR 2.625 with a
412x915 screen.

## Automated evidence

- Final local `pnpm check` at `d54481e`: PASS; 164 Vitest tests in 23 files,
  including 38 GeoNames tests, 3 vector-build plus 22 vector-publication tests,
  and 9 artifact tests. Build and release verification found 24 files, 6
  document assets, and 0 source maps.
- Complete E2E at `2d25b89`, before the text-only fix: desktop 72 passed and 4
  intentional project skips; mobile 69 passed and 7 intentional project skips;
  aggregate 141 passed, 11 skipped, 0 failed.
- The focused Vitest assertion for the English Development question passed at
  `d54481e`.
- Successful pre-copy-fix remote runs:
  [CI 30696841125](https://github.com/0mn1si2i5/Mundus/actions/runs/30696841125)
  and
  [Pages 30696841098](https://github.com/0mn1si2i5/Mundus/actions/runs/30696841098).
- The `d54481e`
  [Pages run 30697975472](https://github.com/0mn1si2i5/Mundus/actions/runs/30697975472)
  and
  [CI run 30697975486](https://github.com/0mn1si2i5/Mundus/actions/runs/30697975486)
  both succeeded. Final evidence-head checks remain pending until this
  documentation is committed and pushed; no final green PR head is claimed.

## Data identity

- GeoNames immutable normalized input: SHA-256
  `49b3f2114d1e71277572b2773cb1e5b8242f3eb22a9e89598eb95b79408c5621`,
  2,814,375 bytes.
- GeoNames runtime asset: SHA-256
  `b48d0d5c34229b2337a979f8362bef3c7075bf67644819fe32ebe691a5d440f4`,
  780,004 bytes, 6,953 records.
- Natural Earth assets retain their documented manifest hashes; build,
  runtime, and release-artifact identity checks passed.

## Manual acceptance checklist

The numbering below matches Section 8 of the approved convergence plan.

1. **PASS:** Direct load and hard reload displayed the parchment globe without
   fatal console or core-request errors on desktop and Pixel emulation.
2. **PASS:** Exact Tokyo selection and antipode remained correct through point
   dragging on desktop and mobile.
3. **PASS:** Beijing and Tokyo searches supported keyboard selection and showed
   the expected origin/antipode city relations and represented-major-city scope
   caveats.
4. **PASS:** Exact and approximate share URLs reloaded with the intended point
   and privacy precision.
5. **PASS:** Other Side, Development, and Sunline preserved the intended
   selected point while switching modes.
6. **PASS:** Development data was absent before first entry, loaded once when
   opened, and remained lazy thereafter.
7. **PASS:** Sunline fixed time, playback, return-to-now, UTC labels, and
   educational caveat worked in both viewports.
8. **PASS:** Chinese and English preserved core meaning, document title and
   language, sources, and units.
9. **PASS:** Primary keyboard controls worked and dialog/Mode Atlas focus
   restoration remained correct.
10. **PASS WITH QUALIFICATION:** Reduced motion, WebGL unavailability, and
    context-loss recovery passed through desktop capability simulations, not on
    physical mobile hardware.
11. **PASS:** Resource inspection selected desktop 50m and Pixel 110m vector
    profiles without loading the other profile in each session.
12. **PASS:** A temporary Node server mounted unchanged `dist` at
    `http://127.0.0.1:58974/Mundus/` by stripping only the `/Mundus` request
    prefix. Desktop and Pixel direct loads and hard reloads succeeded; the
    server was stopped after acceptance.

Under the literal `/Mundus/` mount, every observed `/Mundus/assets` request
returned 200, including index JavaScript/CSS, GeoNames JSON, UNDP JavaScript,
desktop 50m MVG, and Pixel 110m MVG. There were no root-path 404s, non-aborted
request failures, page errors, HTTP status at or above 400, or fatal console
messages. Development remained lazy until first entry. Intentional rapid
navigation produced `ERR_ABORTED` dynamic imports followed by successful loads;
these aborted requests are not classified as failed requests.

## Review disposition

Resolved P1 findings: stale GHSL authority; mutable GeoNames provenance and
offline rebuild; duplicate display ambiguity; transactional/concurrent
publication safety; nondeterministic CI vector quality and drawer assertion;
vector runtime/artifact identity; landscape mobile budget; and causal
Development wording. Independent product/data review found the causal P1 and
confirmed its fix; engineering/release review found no P0/P1. No P0 or P1
remained at drafting, no product implementation P0/P1 remains, and the literal
subpath gate is closed.

Known P2 limitations recorded by PR #5 at candidate SHA `a1aa513`:

- Superseded by the stacked short-cycle hardening: canonical non-Chinese
  fallback names were unmarked in the Chinese UI at `a1aa513`; authenticated
  runtime format v2 now preserves explicit fallback provenance and presents the
  visible, accessible `GeoNames 原名（暂无中文名）` marker without fabricating a
  translation.
- Non-fatal `Three.Clock`, `THREE.Color`, and WebGL `ReadPixels` GPU-stall
  warnings remain.
- Fixed one-Canvas vector-load cancellation/cache-retention optimization is
  deferred; no current failure was observed.
- Headless Pixel emulation is not physical Android or medium-range hardware.
- V1.1 has no deployment or live smoke because a pull-request artifact is not a
  deployment and PR #5 must not deploy.

## Publication boundary

This candidate remains draft and unpublished. There is no authorization to
merge, deploy, tag, or create a Release. V1.0.0 remains live and untouched; a
successful local check or PR Pages artifact does not change that boundary.

## Short-cycle hardening addendum - 2026-08-01

This addendum supersedes only the current-state interpretations above. It does
not rewrite or invalidate the historical measurements at `d54481e`, `2d25b89`,
or `a1aa513`.

- [Stacked PR #6](https://github.com/0mn1si2i5/Mundus/pull/6) converges exactly
  on PR #5 candidate `a1aa513a515b2360dba6cb17f64e37d51ab8afc9`. Its head at
  this checkpoint is `b93a9fbd2c9bdcd5072dd7961f2aff42c85c3024`; the final
  documentation commit will produce a later SHA, so `b93a9fb` is not a terminal
  publication identity.
- GeoNames runtime format v2 is SHA-256
  `68d971df7d66f16eb23a1921623f4608176ed36ec8bb4ac00d1ba57d7493dfab`,
  793,910 bytes and 6,953 rows: 3,862 rows have a real Chinese label and 3,091
  use the canonical source-name fallback. The fallback marker is visible and
  accessible in Chinese. Source selection, row eligibility, relation semantics,
  and ranking did not change.
- The warning matrix used Node.js 22.23.1, pnpm 11.7.0, Playwright 1.61.1,
  Chromium 149, Three.js 0.185.1, React Three Fiber 9.6.1, and Drei 10.7.7. It
  covered desktop and Pixel emulation across root load, vector readiness, drag,
  Development, and Sunline, with and without screenshots. The dependency-owned
  `THREE.Clock` deprecation occurred once per full navigation, with three
  occurrences per profile, from the R3F events ESM stack at `new Clock`; it
  remains a non-blocking third-party P2.
- Two line materials passed a Mundus RGBA string into `THREE.Color`. They now
  use opaque `#43423a`; warning count fell from 6 to 0 per profile while the
  intended raster RGBA styling remains unchanged.
- The current `ReadPixels` matrix recorded zero diagnostics. Earlier
  authenticated desktop evidence recorded two intermittent ANGLE diagnostics;
  Pixel recorded zero. There is no application JavaScript `readPixels` call and
  the warning did not depend on screenshot capture. It remains an intermittent
  browser/GPU P2. The matrix recorded no errors, page errors, request failures,
  or test failures.
- The bounded schema-v3 performance baseline is
  [`docs/performance/2026-08-01-v1.1-publication-candidate.md`](performance/2026-08-01-v1.1-publication-candidate.md).
  It measured implementation SHA
  `79c0f67d0595c4c83e07016f147af80c0ff76a45` in two comparator-matched
  executions and introduced no new budgets. Pixel 7 remains emulation, not
  physical Android evidence.
- Production dependency audit at `2026-08-01T14:59:20Z` used Node.js 22.23.1,
  pnpm 11.7.0, and `pnpm audit --prod --audit-level high`; result: `No known
vulnerabilities found`. No dependency upgrade was made.
- Focused local evidence accumulated during hardening: Task 1 builder 42,
  focused tests 44, browser 1; Task 2 rendering 15, full unit 169, and focused
  desktop plus mobile browser coverage; performance tests 16 and comparator
  pass. These are focused/intermediate results, not the full final gate.
- Final `pnpm check`, complete desktop/mobile E2E, exact-head manual acceptance,
  and remote `quality`, `vector-data-full`, `browser-smoke`, and `pages-artifact`
  checks for the eventual PR #5 final head remain pending. Draft `deploy-pages`
  and `live-smoke` are expected to skip. Earlier successful PR #6 runs are
  intermediate only and are not claimed for the final documentation head.

The executable owner-gated release procedure and final identity readback are in
[`docs/2026-08-01-v1.1-publication-packet.md`](2026-08-01-v1.1-publication-packet.md).
This addendum is preparation evidence, not authorization to merge or publish.
