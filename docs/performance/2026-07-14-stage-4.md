# Stage 4 Sunline release regression · 2026-07-14

## Scope

- Production build with Sunline fixed at `2024-03-20T12:00Z`, point `0,0`
- Independent night shader, civil-twilight band, five latitude guides and subsolar marker active
- UTC controls, 1440× playback, location result and reproducible sharing enabled
- Desktop 1200×760 and mobile 390×844 visual review

## Validation

- `pnpm check`: pass
- Vitest: 15 files, 54 tests passed
- Playwright: 25 passed; one desktop-only lifecycle case intentionally skipped in the mobile project
- Repeated mode lifecycle: 20 switches complete without page errors
- WebGL context loss/restoration regression remains green
- Console: no application errors; the existing Three.js `Clock` deprecation warning originates in the rendering dependency

## Headed desktop benchmark

Environment: local production preview, headed bundled Chromium, 1200×760 viewport, `medium` quality profile.

| Scenario               |   FPS | p95 frame time |
| ---------------------- | ----: | -------------: |
| Fixed Sunline          | 120.5 |         9.0 ms |
| Sunline 1440× playback | 120.0 |         9.1 ms |

The playback path reaches the active display ceiling and does not regress the fixed view. These figures are local hardware evidence, not a substitute for the retained mid-range-phone sample.

## Build and visual evidence

- Sunline controls: 2.17 kB gzip, lazy loaded
- Globe viewport: 5.30 kB gzip; the shader adds no network data asset
- App shell: 28.48 kB gzip
- `output/playwright/stage-4-sunline-desktop.png`
- `output/playwright/stage-4-sunline-mobile.png`
- `output/playwright/stage-4-sunline-benchmark.png`

Desktop and mobile review pass: result content remains readable, the collapsed mobile drawer does not collide with mode navigation, the night side retains country outlines, and the subsolar marker remains camera-facing.

## Gate

Stage 4 release regression: **pass**. The physical iPhone 17 low-profile baseline remains 60.0 fps / p95 17.0 ms from stage 1. A real mid-range phone run remains explicitly non-blocking until such hardware is available.
