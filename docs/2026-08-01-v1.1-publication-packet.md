# Mundus V1.1.0 Parchment Atlas publication packet

Status: **historical preparation packet; Pages publication completed**

Prepared: 2026-08-01

Publication update: PR #5 and stacked PR #6 are merged. The product
implementation entered protected `main` at
`1a9c44700e2154186708772a7773fd8972a7aaf2`; same-SHA CI, Pages deployment,
live smoke, and desktop/mobile manual verification passed on 2026-08-02 at
<https://0mn1si2i5.github.io/Mundus/>. Tag `v1.0.0` and the existing GitHub
Release remain at `a5ff99bc60fb7cd2e6e14f4d3bc4f54e5abfb4a1`. No V1.1 tag
or GitHub Release was created.

The remainder of this packet preserves the pre-publication evidence and
sequence as written. It is no longer an executable handoff and does **not**
authorize repeating merge or deployment steps, tagging, creating a GitHub
Release, changing settings, or modifying the live site.

## 1. Authority and identity

Read `AGENTS.md`, `docs/ROADMAP_HANDOFF.md`, this packet, and
`docs/RELEASE_RUNBOOK.md` before any remote action. Preserve unrelated work and
never rewrite, squash, rebase, force-push, or discard reviewed history while
applying the stacked hardening commits.

| Item                         | Identity and state                                                                                                                                             |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Public V1.0.0 baseline       | `main`, live Pages, tag `v1.0.0`, and GitHub Release all resolve to `a5ff99bc60fb7cd2e6e14f4d3bc4f54e5abfb4a1`                                                 |
| Live site                    | <https://0mn1si2i5.github.io/Mundus/>; still V1.0.0 until an authorized exact-SHA V1.1 deployment passes                                                       |
| Sole eventual V1.1 candidate | Draft [PR #5](https://github.com/0mn1si2i5/Mundus/pull/5), base candidate `a1aa513a515b2360dba6cb17f64e37d51ab8afc9`; it remains unchanged at preparation time |
| Stacked hardening review     | [PR #6](https://github.com/0mn1si2i5/Mundus/pull/6), whose base converges exactly on `a1aa513`; checkpoint head `b93a9fbd2c9bdcd5072dd7961f2aff42c85c3024`     |
| Final candidate              | Runtime readback of the reviewed PR #6 head, then PR #5 `headRefOid`; record the concrete value in the PR body/comment and final report                        |
| Final protected `main`       | Runtime readback of PR #5 `mergeCommit.oid`; it must be nonempty and exactly equal fetched `origin/main`                                                       |
| Final deployment/tag/Release | Must equal that PR #5 merge commit; do not substitute `a1aa513`, `79c0f67`, `b93a9fb`, or a newer `main`                                                       |

PR #6's packet commit necessarily creates a head later than `b93a9fb`. Treat all
listed pre-final SHAs as provenance, never as the terminal release identity.
PR #5 becomes the sole final candidate only after stacked review is complete and
its remote head is fast-forwarded to the exact reviewed PR #6 head. Concrete
future identities belong in the PR body/comment and final release report, not in
edits to this packet.

## 2. Product and data contract

V1.1.0 retains the three existing modes and the static, one-Canvas architecture.
It ships the Parchment Atlas presentation, draggable antipode cross-section,
bilingual GeoNames major-city search, bilateral origin/antipode city relations,
and Natural Earth vector globe. It adds no backend, account, telemetry, new
mode, or Human Morphology runtime.

### Immutable and generated identities

- GeoNames immutable normalized build input:
  `49b3f2114d1e71277572b2773cb1e5b8242f3eb22a9e89598eb95b79408c5621`.
- GeoNames runtime format v2:
  `68d971df7d66f16eb23a1921623f4608176ed36ec8bb4ac00d1ba57d7493dfab`,
  793,910 bytes, 6,953 rows, 3,091 canonical-name fallbacks, and 3,862 real
  Chinese labels.
- Natural Earth vector globe 110m:
  `7d58a14dc5a35dd8ff9fe7c56a37bbfaf6182975cab2b29b76769d06bffefcce`.
- Natural Earth vector globe 50m:
  `fa3cd1703aaa78ef9e05e0e20972e5afbb7bbf04b6617af735713fd09fc5bc52`.
- Development source is UNDP Human Development Report 2025, complete time
  series 1990-2023. The tracked manifest source SHA-256 is
  `61ed82e5b66c88dfca8ff9fac775c63981ecab6a254862af97acacc41c143117`
  and its generated derived-asset SHA-256 is
  `182b3dfb654d538e2ce99fdc42200209204785b5c9ee980858db20c42f447c6f`.
  The production performance report separately records emitted Development data
  identity `40d033f6650a9ca21d2415952959654bc5a30545066970f4f66f93f6de673b92`.

Runtime v2 preserves provenance for Chinese fallback labels. The visible and
accessible `GeoNames 原名（暂无中文名）` marker discloses a canonical source name
without inventing a translation. The hardening did not change the GeoNames
source snapshot, row eligibility, search ranking, or bilateral relation scope.
Results remain represented major-city entries, not nearest settlements or an
administrative authority.

## 3. Release-note drafts

These drafts are concise and ready for owner editing. Keep the source and scope
caveats in the final notes.

### English

**Mundus V1.1.0 - Parchment Atlas** refines the interactive Earth exhibit with a
warmer parchment presentation and a draggable Other Side cross-section. Search
the reviewed GeoNames major-city snapshot in English or Chinese, compare
bilateral origin/antipode city relations with explicit coverage caveats, and
explore the globe through bounded 110m/50m Natural Earth vector geometry. The
three established lenses, privacy-aware sharing, accessible fallbacks, and
static client architecture remain intact. Data identities, licenses, methods,
and educational limitations are documented in the repository.

Known non-blocking limitations: Pixel 7 results are browser emulation rather
than physical Android evidence; a dependency-owned `THREE.Clock` deprecation
warning and intermittent browser/GPU `ReadPixels` diagnostic may appear without
page errors or observed product failure.

### 中文

**Mundus V1.1.0 - 羊皮纸地图集**以更温暖的羊皮纸视觉完善互动地球展品，并为“地球另一端”加入可拖拽剖面。用户可用中文或英文检索经审核的 GeoNames 主要城市快照，在明确的覆盖范围说明下查看起点与对跖点两侧的城市关系，并通过有界的 Natural Earth 110m/50m 矢量几何探索地球。既有三种观察模式、注重隐私的分享、无障碍降级路径和纯静态客户端架构保持不变。数据身份、许可、方法与教育用途限制均记录在仓库中。

已知非阻断限制：Pixel 7 结果来自浏览器模拟，并非 Android 真机；依赖库自身的 `THREE.Clock` 弃用警告和浏览器/GPU 偶发的 `ReadPixels` 诊断仍可能出现，但现有证据中没有页面错误或产品故障。

## 4. Severity disposition

### P0

No known P0 at packet preparation. Any secret exposure, prohibited or
mislicensed data, corrupt/non-reproducible artifact, or identity mismatch found
later is a mandatory publication stop.

### P1

Previously identified candidate P1 findings are recorded as resolved in
`docs/2026-08-01-v1.1-parchment-atlas-candidate.md`. No known implementation P1
remains at preparation. The still-pending final local gates, exact-head manual
acceptance, required PR-head `quality`, `vector-data-full`, `browser-smoke`, and
`pages-artifact` checks, protected-main Pages deployment, and live smoke are
release gates: a failure or missing result is P1 and blocks publication.

### P2

- Dependency-owned `THREE.Clock` deprecation: once per full navigation and
  three times per warning profile, with the R3F events ESM stack ending at
  `new Clock`. No application-owned fix or dependency upgrade is justified for
  this release.
- Intermittent browser/GPU `ReadPixels` diagnostic: current matrix zero; prior
  authenticated desktop matrix two ANGLE diagnostics and Pixel zero. There is
  no application JavaScript `readPixels` call and screenshot capture did not
  control occurrence.
- One-Canvas vector cancellation/cache-retention optimization remains deferred;
  no current failure was observed.
- Pixel 7 coverage is Chromium emulation on macOS, not physical Android or
  medium-range hardware. Record physical Android testing as absent unless it is
  actually completed against the exact final candidate.

The owned color warning is resolved, not a remaining P2: two line materials now
use opaque `#43423a`, reducing `THREE.Color` warnings from 6 to 0 per profile
while preserving raster RGBA usage.

## 5. Evidence ledger

Historical details remain in
`docs/2026-08-01-v1.1-parchment-atlas-candidate.md`; do not rewrite those
measurements when newer evidence is appended.

| Surface                   | Current evidence                                                                                                                                                                                                 | Final requirement                                                                                                             |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| Task 1 marker/data        | Builder 42, focused 44, browser 1; runtime v2 and fallback marker verified; no source or ranking drift                                                                                                           | Included in final `pnpm check` and complete E2E                                                                               |
| Task 2 rendering/warnings | Rendering 15, full unit 169, focused desktop and mobile; owned color warning 6 to 0/profile                                                                                                                      | Repeat warning/manual matrix at exact final candidate if later code affects rendering                                         |
| Performance harness       | 16 tests and comparator pass                                                                                                                                                                                     | Preserve report; no new budgets                                                                                               |
| Performance measurement   | Schema v3 at exact implementation SHA `79c0f67d0595c4c83e07016f147af80c0ff76a45`; execution IDs `26806305-a6d6-46c5-a57c-f78ea86024f8` and `1e7e2e7d-a898-4928-a635-6ceffb7cf70a`; comparator provenance matched | No rerun needed for documentation-only commits; rerun if production behavior or assets change                                 |
| Dependency audit          | `2026-08-01T14:59:20Z`, Node.js 22.23.1, pnpm 11.7.0, `pnpm audit --prod --audit-level high`: `No known vulnerabilities found`; no upgrade                                                                       | Recheck only if lockfile/dependencies change or owner requests fresher evidence                                               |
| Final local non-browser   | Pending                                                                                                                                                                                                          | `pnpm check` at exact final PR #5 head                                                                                        |
| Final browser             | Pending                                                                                                                                                                                                          | Complete repository-owned desktop/mobile `pnpm test:e2e` at exact final PR #5 head                                            |
| Final manual              | Pending                                                                                                                                                                                                          | All checklist items below at exact final PR #5 head; physical Android status stated honestly                                  |
| Final PR checks           | Pending for the eventual PR #5 final head                                                                                                                                                                        | Required `quality`, `vector-data-full`, `browser-smoke`, and `pages-artifact` successful; inspect logs and artifact semantics |
| Pages and live            | Not run for V1.1; PR artifacts do not deploy                                                                                                                                                                     | Only after authorized protected-main merge: `deploy-pages` and desktop/mobile `live-smoke` successful at exact final main SHA |

The warning diagnosis environment was Node.js 22.23.1, pnpm 11.7.0,
Playwright 1.61.1, Chromium 149, Three.js 0.185.1, React Three Fiber 9.6.1,
and Drei 10.7.7. It covered desktop and Pixel root load, vector readiness, drag,
Development, and Sunline, with and without screenshots. The current matrix had
zero errors, page errors, request failures, test failures, or `ReadPixels`
diagnostics.

The bounded performance report is
`docs/performance/2026-08-01-v1.1-publication-candidate.md`. Its loopback,
headless, Pixel-emulated measurements are not Pages latency, constrained-network
evidence, physical GPU/thermal evidence, or new performance budgets.

### Exact-head local and manual gate

Run only after PR #6 review is complete and PR #5 has been fast-forwarded to its
exact reviewed head. Record command time, exact `git rev-parse HEAD`, result,
and test counts in the PR body/comment before requesting publication review.

```bash
node --version
pnpm --version
git status --short
git rev-parse HEAD
pnpm install --frozen-lockfile
pnpm check
pnpm test:e2e
pnpm release:verify
pnpm release:serve
```

`pnpm release:serve` serves the already-built, unchanged `dist` at the printed
literal `/Mundus/` URL and shuts down on `Ctrl-C`, `SIGINT`, or `SIGTERM`. It
does not build or alter the artifact. If any build runs after
`pnpm release:verify`, artifact identity is invalidated: stop, rerun the complete
local gate and artifact verification, then restart manual acceptance. Against
that unchanged artifact, perform desktop Chromium and Pixel 7-class emulation
checks:

1. Direct load and hard reload; no fatal console, page, or core-request errors.
2. Select and drag a point; verify lifecycle, exact antipode, and fallback DOM.
3. Search representative English, real-Chinese, and fallback-name cities by
   keyboard; verify marker accessibility, ranking stability, bilateral relations,
   and represented-major-city caveat.
4. Reload precise and approximate share URLs.
5. Switch Other Side, Development, and Sunline while preserving geographic
   context.
6. Confirm Development assets remain absent until first entry; verify median,
   history, contrast wording, source, units, and missing state.
7. Verify fixed Sunline time, playback, return to now, UTC labels, and caveat.
8. Verify Chinese/English title, language, core meaning, sources, and units.
9. Exercise keyboard controls, dialog/Mode Atlas focus restoration, and reduced
   motion.
10. Simulate WebGL unavailability and context loss; verify meaningful fallback
    and recovery.
11. Confirm desktop 50m and Pixel 110m selection without loading the other
    profile; inspect warning disposition and resource cleanup.
12. Confirm all assets resolve below `/Mundus/`, with no root-relative breakage,
    forbidden source maps, or missing notices/licenses.

If physical Android is unavailable, say `not tested` rather than extending
Pixel emulation claims. A physical-device failure in a primary flow is P1; a
missing sample remains the recorded P2 hardware gap.

### Final PR readback

Earlier successful PR #6 checks are useful intermediate evidence but never
prove the eventual PR #5 final head. The future integration commands below are
owner-gated and must not run during packet preparation. They fail closed unless
PR #5 still points to exact base candidate `a1aa513`, PR #6 descends from that
commit, and PR #5 is updated by fast-forward only to the exact reviewed PR #6
head. Cherry-pick, squash, rebase, force-push, or any different history is
prohibited.

```bash
EXPECTED_PR5_HEAD='a1aa513a515b2360dba6cb17f64e37d51ab8afc9'
PR5_BRANCH='codex/v1.1-parchment-convergence'
PR5_HEAD="$(gh pr view 5 --json headRefOid --jq '.headRefOid')"
test -n "$PR5_HEAD"
test "$PR5_HEAD" = "$EXPECTED_PR5_HEAD"

PR6_HEAD="$(gh pr view 6 --json headRefOid --jq '.headRefOid')"
test -n "$PR6_HEAD"
git fetch origin "$EXPECTED_PR5_HEAD" "$PR6_HEAD"
git merge-base --is-ancestor "$EXPECTED_PR5_HEAD" "$PR6_HEAD"
test "$(git merge-base "$EXPECTED_PR5_HEAD" "$PR6_HEAD")" = "$EXPECTED_PR5_HEAD"

git fetch origin "$PR5_BRANCH"
test "$(git rev-parse "origin/$PR5_BRANCH")" = "$EXPECTED_PR5_HEAD"
git merge-base --is-ancestor "origin/$PR5_BRANCH" "$PR6_HEAD"
git push origin "$PR6_HEAD:refs/heads/$PR5_BRANCH"

PR5_HEAD="$(gh pr view 5 --json headRefOid --jq '.headRefOid')"
test -n "$PR5_HEAD"
test "$PR5_HEAD" = "$PR6_HEAD"
gh pr view 5 --json url,state,isDraft,baseRefName,baseRefOid,headRefName,headRefOid,mergeable,mergeStateStatus,reviewDecision,statusCheckRollup
gh run list --branch "$PR5_BRANCH" --limit 12
```

Any failed assertion or non-fast-forward result stops the procedure for the
owner. Record `PR6_HEAD` and matching PR #5 `headRefOid` in the PR body/comment
and later final report; do not edit this packet with the value. Require
exact-head `quality`, `vector-data-full`, `browser-smoke`, and `pages-artifact`
to succeed. Draft PR `deploy-pages` and `live-smoke` must skip by design. A
successful `pages-artifact` is rehearsal evidence, not a deployment.

## 6. Owner-gated publication sequence

This section is a future procedure, not an instruction to execute now.

1. **Stacked review:** Review PR #6 at its final packet head. Resolve valid
   findings and require exact-head `quality`, `vector-data-full`,
   `browser-smoke`, and `pages-artifact` to succeed. Draft `deploy-pages` and
   `live-smoke` remain expected skips. Do not merge PR #6 to `main`.
2. **Converge without rewriting:** First assert PR #5 remote `headRefOid` is
   still exact `a1aa513a515b2360dba6cb17f64e37d51ab8afc9`. Assert the reviewed
   PR #6 final head descends from that exact commit, then update
   `codex/v1.1-parchment-convergence` by fast-forward only to the exact PR #6
   head. Do not cherry-pick, squash, rebase, force-push, or alter commits. Read
   PR #5 back and require its `headRefOid` to equal the reviewed PR #6 head.
   Any mismatch or non-fast-forward result stops for the owner.
3. **Sole candidate:** Confirm PR #5 alone contains the complete V1.1 candidate,
   remains Draft, and has clean exact-head local, E2E, manual, data, artifact,
   dependency, and successful `quality`, `vector-data-full`, `browser-smoke`,
   and `pages-artifact` evidence. Confirm Draft `deploy-pages` and `live-smoke`
   skipped as expected. Close or otherwise retire stacked review PR #6 only
   according to owner direction; do not rewrite its review history.
4. **Explicit owner gate:** Present the exact PR #5 URL and candidate SHA, all
   local gate results and URLs, exact-head `quality`, `vector-data-full`,
   `browser-smoke`, and `pages-artifact` results, expected Draft `deploy-pages`
   and `live-smoke` skips, unresolved P0/P1/P2 disposition, physical Android
   status, source/data identities, and proposed publication actions. Obtain
   explicit owner authorization to mark Ready, merge, and publish that exact
   candidate. Preparation, approval of this document, green `quality`,
   `vector-data-full`, `browser-smoke`, and `pages-artifact` checks, or prior
   publication authority is not authorization.
5. **Protected final main:** Only after that authorization, mark PR #5 Ready and
   merge through protected `main` without bypassing `quality`,
   `vector-data-full`, `browser-smoke`, or `pages-artifact`, and without
   rewriting. GitHub merge method must be **Create a merge commit**; squash and
   rebase merges are prohibited. Read the final release SHA only from nonempty
   PR #5 `mergeCommit.oid`, fetch it, and require fetched `origin/main` to equal
   it exactly. Never adopt a different, newer `main`; any mismatch stops for the
   owner.
6. **Pages exact SHA:** Wait for the repository-owned Pages workflow on that
   protected-main SHA. Locate only the Pages `push` run for that exact commit.
   Require build/artifact verification, `deploy-pages`, and desktop/mobile
   `live-smoke` to pass, and inspect the exact run's `headSha` and jobs. Then
   read the canonical Pages URL from the Pages API and perform the live manual
   product smoke there. The canonical Pages URL is not claimed as the
   `deploy-pages` output URL.
7. **Tag and Release only afterward:** Only when final main, deployed SHA, and
   live evidence agree, create annotated tag `v1.1.0` at the exact PR #5 merge
   commit, push it, verify the remote peeled tag, and create the GitHub Release
   from that tag.
   Use the bilingual-ready notes above, owner-approved wording, evidence links,
   and known P2s. Never tag a PR head or an earlier deployment.
8. **Terminal readback:** Complete every identity check below. If any SHA, URL,
   gate, or public surface disagrees, the V1.1 release is incomplete.

## 7. Owner-authorized publication commands and identity readback

Every command in this section is owner-gated future procedure and must not run
during packet preparation. The blocks fail closed on missing or mismatched
runtime readbacks. Do not type a release SHA from memory and do not adopt ambient
or newer `main`.

The explicitly authorized GitHub merge action must select **Create a merge
commit**. Only after explicit owner authorization, the future sequence below
reads and validates both reviewed heads, marks PR #5 Ready, merges with the
reviewed head bound, and only then derives the final SHA from the merge result.
Do not use `--squash` or `--rebase`. Stop if any assertion, branch protection, or
GitHub cannot perform that exact sequence and merge method.

```bash
PR6_HEAD="$(gh pr view 6 --json headRefOid --jq '.headRefOid')"
test -n "$PR6_HEAD"
PR5_HEAD="$(gh pr view 5 --json headRefOid --jq '.headRefOid')"
test -n "$PR5_HEAD"
test "$PR5_HEAD" = "$PR6_HEAD"

gh pr ready 5
PR5_IS_DRAFT="$(gh pr view 5 --json isDraft --jq '.isDraft')"
test "$PR5_IS_DRAFT" = 'false'
PR5_HEAD="$(gh pr view 5 --json headRefOid --jq '.headRefOid')"
test -n "$PR5_HEAD"
test "$PR5_HEAD" = "$PR6_HEAD"
gh pr merge 5 --merge --match-head-commit "$PR5_HEAD"

PR5_STATE="$(gh pr view 5 --json state --jq '.state')"
test "$PR5_STATE" = 'MERGED'
FINAL_V1_1_MAIN_SHA="$(gh pr view 5 --json mergeCommit --jq '.mergeCommit.oid')"
test -n "$FINAL_V1_1_MAIN_SHA"
test "${#FINAL_V1_1_MAIN_SHA}" -eq 40

git fetch origin "$FINAL_V1_1_MAIN_SHA"
git fetch origin main
test "$(git rev-parse origin/main)" = "$FINAL_V1_1_MAIN_SHA"
gh pr view 5 --json url,state,mergedAt,mergeCommit,headRefOid,baseRefOid,statusCheckRollup
```

Locate only the successful Pages push run for the exact merge commit. Require one
matching successful run, then inspect its exact head and jobs before any tag:

```bash
PAGES_RUN_ID="$(gh run list --workflow Pages --event push --commit "$FINAL_V1_1_MAIN_SHA" --limit 20 --json databaseId,conclusion,headSha --jq '[.[] | select(.conclusion == "success")] | if length == 1 then .[0].databaseId else empty end')"
test -n "$PAGES_RUN_ID"
PAGES_HEAD_SHA="$(gh run view "$PAGES_RUN_ID" --json headSha --jq '.headSha')"
PAGES_CONCLUSION="$(gh run view "$PAGES_RUN_ID" --json conclusion --jq '.conclusion')"
PAGES_JOBS_VALID="$(gh run view "$PAGES_RUN_ID" --json jobs --jq '([.jobs[] | select(.name == "pages-artifact" and .conclusion == "success")] | length == 1) and ([.jobs[] | select(.name == "deploy-pages" and .conclusion == "success")] | length == 1) and ([.jobs[] | select(.name == "live-smoke" and .conclusion == "success")] | length == 1)')"
test "$PAGES_HEAD_SHA" = "$FINAL_V1_1_MAIN_SHA"
test "$PAGES_CONCLUSION" = 'success'
test "$PAGES_JOBS_VALID" = 'true'
gh run view "$PAGES_RUN_ID" --json url,status,conclusion,headSha,jobs

CANONICAL_PAGES_URL="$(gh api repos/0mn1si2i5/Mundus/pages --jq '.html_url')"
test -n "$CANONICAL_PAGES_URL"
MUNDUS_LIVE_URL="$CANONICAL_PAGES_URL" pnpm exec playwright test --config=playwright.live.config.ts
```

Only after exact-SHA Pages and live checks pass, and only under explicit owner
authorization:

```bash
RELEASE_NOTES_FILE='tmp/v1.1.0-owner-approved-release-notes.md'
test -n "$RELEASE_NOTES_FILE"
test -f "$RELEASE_NOTES_FILE"
git tag -a v1.1.0 "$FINAL_V1_1_MAIN_SHA" -m "Mundus V1.1.0 Parchment Atlas"
git push origin v1.1.0
REMOTE_TAG_LINE="$(git ls-remote origin 'refs/tags/v1.1.0^{}')"
test -n "$REMOTE_TAG_LINE"
REMOTE_TAG_SHA="${REMOTE_TAG_LINE%%$'\t'*}"
test -n "$REMOTE_TAG_SHA"
test "$REMOTE_TAG_SHA" = "$FINAL_V1_1_MAIN_SHA"
gh release create v1.1.0 --verify-tag --title "Mundus V1.1.0 - Parchment Atlas" --notes-file "$RELEASE_NOTES_FILE"
```

Read back all terminal identities:

```bash
test "$(git rev-parse origin/main)" = "$FINAL_V1_1_MAIN_SHA"
REMOTE_TAG_LINE="$(git ls-remote origin 'refs/tags/v1.1.0^{}')"
test -n "$REMOTE_TAG_LINE"
REMOTE_TAG_SHA="${REMOTE_TAG_LINE%%$'\t'*}"
test -n "$REMOTE_TAG_SHA"
test "$REMOTE_TAG_SHA" = "$FINAL_V1_1_MAIN_SHA"
RELEASE_TAG="$(gh release view v1.1.0 --json tagName --jq '.tagName')"
RELEASE_URL="$(gh release view v1.1.0 --json url --jq '.url')"
RELEASE_IS_DRAFT="$(gh release view v1.1.0 --json isDraft --jq '.isDraft')"
RELEASE_IS_PRERELEASE="$(gh release view v1.1.0 --json isPrerelease --jq '.isPrerelease')"
test "$RELEASE_TAG" = 'v1.1.0'
test -n "$RELEASE_URL"
test "$RELEASE_IS_DRAFT" = 'false'
test "$RELEASE_IS_PRERELEASE" = 'false'
gh release view v1.1.0 --json url,tagName,targetCommitish,isDraft,isPrerelease
gh run view "$PAGES_RUN_ID" --json url,conclusion,headSha,jobs
gh api repos/0mn1si2i5/Mundus/pages
gh repo view 0mn1si2i5/Mundus --json url,homepageUrl,defaultBranchRef
curl --fail --location --silent --show-error "$CANONICAL_PAGES_URL"
```

Checklist:

- [ ] `origin/main` equals `FINAL_V1_1_MAIN_SHA`.
- [ ] PR #5 exact-head `quality`, `vector-data-full`, `browser-smoke`, and
      `pages-artifact` all succeeded; Draft `deploy-pages` and `live-smoke`
      skipped as expected.
- [ ] Successful Pages run `headSha` equals `FINAL_V1_1_MAIN_SHA`.
- [ ] Remote annotated `v1.1.0` peeled commit from `git ls-remote` equals the PR
      #5 merge commit.
- [ ] GitHub Release resolves through `v1.1.0` to the same commit and is public,
      non-draft, and non-prerelease unless the owner explicitly chose otherwise.
- [ ] Canonical Pages URL read from the Pages API after the exact-SHA deployment
      is healthy and exact-SHA desktop/mobile live smoke plus manual smoke
      passed; it is not represented as the `deploy-pages` output URL.
- [ ] English and Chinese README live links resolve to the verified Pages URL.
- [ ] Repository `homepageUrl` equals the verified canonical live URL.
- [ ] Final report records PR #5 exact-head `quality`, `vector-data-full`,
      `browser-smoke`, and `pages-artifact`; expected Draft `deploy-pages` and
      `live-smoke` skips; final-main Pages and live-smoke; tag; Release; local
      test counts; manual environment; data identities; dependency audit; and
      P2s.

## 8. Rollback

If the V1.1 deployment or live product develops a P0/P1, do not edit Pages files
or upload local `dist` manually. The following is owner-gated future procedure,
not a command to run during preparation. Select only a successful Pages `push`
run with a nonempty expected rollback SHA, matching `headSha`, and durable prior
`live-smoke` success evidence. Rerunning rebuilds and redeploys that same source
SHA through the current repository workflow; it does not reuse old artifact
bytes. Capture the prior attempt, wait for the rerun, require the attempt to
increment, and validate the latest attempt completely. If any assertion or
evidence check fails, stop for the owner.

```bash
ROLLBACK_SHA='a5ff99bc60fb7cd2e6e14f4d3bc4f54e5abfb4a1'
test -n "$ROLLBACK_SHA"
ROLLBACK_RUN_ID="$(gh run list --workflow Pages --event push --commit "$ROLLBACK_SHA" --limit 20 --json databaseId,conclusion,headSha --jq '[.[] | select(.conclusion == "success")] | if length >= 1 then .[0].databaseId else empty end')"
test -n "$ROLLBACK_RUN_ID"
test "$(gh run view "$ROLLBACK_RUN_ID" --json headSha --jq '.headSha')" = "$ROLLBACK_SHA"
test "$(gh run view "$ROLLBACK_RUN_ID" --json conclusion --jq '.conclusion')" = 'success'
ROLLBACK_JOBS_VALID="$(gh run view "$ROLLBACK_RUN_ID" --json jobs --jq '([.jobs[] | select(.name == "pages-artifact" and .conclusion == "success")] | length == 1) and ([.jobs[] | select(.name == "deploy-pages" and .conclusion == "success")] | length == 1) and ([.jobs[] | select(.name == "live-smoke" and .conclusion == "success")] | length == 1)')"
test "$ROLLBACK_JOBS_VALID" = 'true'
gh run view "$ROLLBACK_RUN_ID" --json url,status,conclusion,headSha,jobs

ROLLBACK_PRIOR_ATTEMPT="$(gh run view "$ROLLBACK_RUN_ID" --json attempt --jq '.attempt')"
test -n "$ROLLBACK_PRIOR_ATTEMPT"
test "$ROLLBACK_PRIOR_ATTEMPT" -ge 1
gh run rerun "$ROLLBACK_RUN_ID"
gh run watch "$ROLLBACK_RUN_ID" --exit-status

ROLLBACK_NEW_ATTEMPT="$(gh run view "$ROLLBACK_RUN_ID" --json attempt --jq '.attempt')"
test -n "$ROLLBACK_NEW_ATTEMPT"
test "$ROLLBACK_NEW_ATTEMPT" -eq "$((ROLLBACK_PRIOR_ATTEMPT + 1))"
ROLLBACK_NEW_HEAD="$(gh run view "$ROLLBACK_RUN_ID" --attempt "$ROLLBACK_NEW_ATTEMPT" --json headSha --jq '.headSha')"
ROLLBACK_NEW_CONCLUSION="$(gh run view "$ROLLBACK_RUN_ID" --attempt "$ROLLBACK_NEW_ATTEMPT" --json conclusion --jq '.conclusion')"
ROLLBACK_NEW_JOBS_VALID="$(gh run view "$ROLLBACK_RUN_ID" --attempt "$ROLLBACK_NEW_ATTEMPT" --json jobs --jq '([.jobs[] | select(.name == "pages-artifact" and .conclusion == "success")] | length == 1) and ([.jobs[] | select(.name == "deploy-pages" and .conclusion == "success")] | length == 1) and ([.jobs[] | select(.name == "live-smoke" and .conclusion == "success")] | length == 1)')"
test "$ROLLBACK_NEW_HEAD" = "$ROLLBACK_SHA"
test "$ROLLBACK_NEW_CONCLUSION" = 'success'
test "$ROLLBACK_NEW_JOBS_VALID" = 'true'
gh run view "$ROLLBACK_RUN_ID" --attempt "$ROLLBACK_NEW_ATTEMPT" --json url,status,conclusion,headSha,jobs

ROLLBACK_CANONICAL_URL="$(gh api repos/0mn1si2i5/Mundus/pages --jq '.html_url')"
test -n "$ROLLBACK_CANONICAL_URL"
MUNDUS_LIVE_URL="$ROLLBACK_CANONICAL_URL" pnpm exec playwright test --config=playwright.live.config.ts
```

Current GitHub CLI exposes `attempt` in `gh run view --json`. If the installed
CLI does not, replace each attempt read above with
`gh api "repos/0mn1si2i5/Mundus/actions/runs/$ROLLBACK_RUN_ID" --jq '.run_attempt'`;
all nonempty and increment assertions remain mandatory.

The repository live-smoke command covers its configured desktop and mobile
projects. Record the rollback run URL, rollback SHA, prior and new attempt
numbers, rebuilt/redeployed same-SHA result, latest job evidence, canonical URL,
and post-rerun desktop/mobile live-smoke result.

Immediately open a reviewed revert or corrective PR so protected `main` again
represents intended production. Apply normal required checks, deployment, live
smoke, and identity readback to the corrective commit. Do not move or delete a
published tag silently; any release metadata correction requires explicit owner
direction and a durable incident record.

## 9. Preparation boundary

This document, its future commit, focused test results, a successful PR
artifact, prior green PR #6 runs, or owner review of prose are **preparation, not
publication authorization**. At packet preparation:

- V1.0.0 remains the only live and released version;
- PR #5 remains Draft and unchanged at `a1aa513`;
- PR #6 is stacked review work and its eventual final head is not yet known;
- final local/E2E/manual gates and remote final-head `quality`,
  `vector-data-full`, `browser-smoke`, and `pages-artifact` are pending; Draft
  `deploy-pages` and `live-smoke` remain expected skips;
- no V1.1 Pages deployment or live smoke exists;
- no `v1.1.0` tag or GitHub Release may be created.

Stop and ask the product owner with exact evidence before Ready/merge/publication.

## 10. Interaction-Clarity Supersession

A later interaction-clarity correction supersedes the current Share UI with one
canonical-precision location link and explicit location privacy disclosure.
Valid historical whole-degree links remain readable. The numbered
pre-publication checklist above is retained as the historical procedure that was
executed for the original V1.1 publication candidate; future validation should
test the canonical link plus legacy whole-degree compatibility instead of
restoring the removed precision choice.
